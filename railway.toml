[build]
builder = "NIXPACKS"

[phases.setup]
# Ép cài đặt PHP và Extension MySQL
nixPkgs = ["php", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "composer"]

[deploy]
startCommand = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"